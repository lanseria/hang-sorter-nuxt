# AI

当前项目为[hang-sorter-nuxt]
github地址为 https://github.com/lanseria/hang-sorter-nuxt
已经考虑了下面条件：
核心框架: Nuxt + Vue 3
语言: TypeScript
样式方案: Unocss CSS
Auto-imports 机制
无主题模式(以浅色模式设计)
移动端布局
严格按我的代码风格写
请不要使用传统的diff + / - 格式。请用一种清晰的文本描述格式来展示代码变更，明确指出‘从哪一行开始’到‘哪一行结束’的旧代码块，应该被替换为新的代码块
下面回答全部使用中文回答

```bash
python3 ./ai/print_project_files.py . "\.(vue|ts|js|json|toml|md)$|Dockerfile$|eslint\.config\.js$" -o ./ai/project_context.txt -e "node_modules,.git,.nuxt,dist,build,public/assets,pnpm-lock.yaml,.vscode,ai,.output"
```

将当前项目转为移动端布局
表格在顶部1/2显示，下面为展示区域，底部有两按钮，一个配置，一个播放
然后修复表格的宽度，第一列尽可能窄，第二列虽然一开始空白，但尽可能宽，
当开始时，展出来的东西可以拖动到这五行中的任意一行，拖动结束后就直接显示在对应行的第二列，拖动元素消失，出现第二个可以拖动的元素。以此类推